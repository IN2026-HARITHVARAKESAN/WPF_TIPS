<Window x:Class="StudentDataViewer.View.DeleteConfirmationView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:StudentDataViewer.View"
        xmlns:res ="clr-namespace:StudentDataViewer.Resources"
        mc:Ignorable="d"
        WindowStartupLocation="CenterOwner"
        Title="DeleteConfirmationView"
        AutomationProperties.AutomationId="DeleteConfirmationView" Height="300" Width="400">
    <Grid>
        <StackPanel Margin="20">
            <TextBlock Text="Are you sure you want to delete this student?"
                   FontSize="16" Margin="0,0,0,20"/>
            <ListView ItemsSource="{Binding SelectedStudents}" Width="354">
                <ListView.View>
                    <GridView>
                        <GridViewColumn Header="ID" DisplayMemberBinding="{Binding StudentId}" Width="80"/>
                        <GridViewColumn Header="Name" DisplayMemberBinding="{Binding Name}" Width="150"/>
                        <GridViewColumn Header="Department" DisplayMemberBinding="{Binding Department}" Width="120"/>
                    </GridView>
                </ListView.View>
            </ListView>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button x:Name="Confirmationyes" Width="130" Height="30" Background="Transparent" Foreground="{StaticResource StandardWhite}" ToolTip="Add new student" BorderThickness="0" Padding="0" Command="{Binding ConfirmCommand}">
                    <Border Background="{StaticResource PrimaryGreen}" CornerRadius="10" BorderThickness="2" Padding="10,5">
                        <TextBlock Text="{x:Static res:Strings.ButtonYes}" Foreground="{StaticResource StandardWhite}" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                </Button>
                <Button x:Name="ConfirmationNo" Width="130" Height="30" Background="Transparent" Foreground="{StaticResource StandardWhite}" ToolTip="Add new student" BorderThickness="0" Padding="0" Command="{Binding CancelCommand}">
                    <Border Background="{StaticResource Red}" CornerRadius="10" BorderThickness="2" Padding="10,5">
                        <TextBlock Text="{x:Static res:Strings.ButtonNo}" Foreground="{StaticResource StandardWhite}" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                </Button>
            </StackPanel>
        </StackPanel>
        
    </Grid>
</Window>
